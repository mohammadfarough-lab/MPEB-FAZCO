'use client'

import { Check, ChevronDown, Search, SlidersHorizontal, X } from 'lucide-react'
import { useMemo, useState } from 'react'
import Link from 'next/link'

type Product = { id: string; name: string; slug: string; description: string; category: string; image: string; video: string; priceInCents: number; stock: number }
const formatPrice = (cents: number) => new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(cents / 100)

export function ShopClient({ products, settings = {} }: { products: Product[]; settings?: Record<string, string> }) {
  const copy = { eyebrow: settings.shop_eyebrow || '/ Fazco shop', title: settings.shop_title || 'Objects that shape the room.', description: settings.shop_description || 'A considered selection of lighting for homes, studios and spaces with a point of view.', search: settings.shop_search_placeholder || 'Search products, categories, or details', category: settings.shop_category_label || 'Filter by category', empty: settings.shop_empty_message || 'No products match your filters.' }
  const [query, setQuery] = useState('')
  const [category, setCategory] = useState('All categories')
  const [categoryOpen, setCategoryOpen] = useState(false)
  const categories = useMemo(() => ['All categories', ...Array.from(new Set(products.map((product) => product.category.trim()).filter(Boolean))).sort()], [products])
  const filteredProducts = useMemo(() => { const normalized = query.trim().toLowerCase(); return products.filter((product) => { const matchesCategory = category === 'All categories' || product.category === category; const matchesQuery = !normalized || `${product.name} ${product.description} ${product.category}`.toLowerCase().includes(normalized); return matchesCategory && matchesQuery }) }, [category, products, query])
  return <div className="mx-auto max-w-7xl px-6 py-20 md:px-10"><div className="mb-10 max-w-2xl"><p className="mb-5 text-sm uppercase tracking-[.16em] text-primary">{copy.eyebrow}</p><h1 className="text-5xl font-medium tracking-tight md:text-7xl">{copy.title}</h1><p className="mt-6 text-base leading-8 text-muted-foreground">{copy.description}</p></div><div className="glass-card mb-8 flex flex-col gap-3 rounded-3xl p-3 md:flex-row md:items-center"><label className="glass-input flex min-h-12 flex-1 items-center gap-3 rounded-2xl px-4"><Search className="size-4 text-primary" aria-hidden="true" /><span className="sr-only">Search products</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder={copy.search} className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-muted-foreground" />{query && <button type="button" onClick={() => setQuery('')} aria-label="Clear search" className="rounded-full p-1 text-muted-foreground hover:bg-muted hover:text-foreground"><X className="size-4" /></button>}</label><div className="relative md:min-w-56"><button type="button" className="glass-input flex min-h-12 w-full items-center gap-3 rounded-2xl px-4 text-left text-sm" onClick={() => setCategoryOpen((open) => !open)} aria-expanded={categoryOpen} aria-haspopup="listbox"><SlidersHorizontal className="size-4 shrink-0 text-primary" aria-hidden="true" /><span className="flex-1 truncate">{category}</span><ChevronDown className={`size-4 shrink-0 text-muted-foreground transition-transform ${categoryOpen ? 'rotate-180' : ''}`} aria-hidden="true" /></button>{categoryOpen && <div className="glass-category-menu absolute inset-x-0 top-[calc(100%+0.5rem)] z-20 overflow-hidden rounded-2xl p-1" role="listbox" aria-label={copy.category}>{categories.map((item) => <button key={item} type="button" role="option" aria-selected={category === item} className="glass-category-option flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm" onClick={() => { setCategory(item); setCategoryOpen(false) }}>{category === item && <Check className="size-4 text-primary" />}{item}</button>)}</div>}</div></div>{filteredProducts.length === 0 ? <div className="rounded-2xl border border-dashed border-border p-10 text-center text-sm text-muted-foreground">{copy.empty}</div> : <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filteredProducts.map((product) => <Link key={product.id} href={`/shop/${product.slug}`} className="glass-card group overflow-hidden rounded-3xl transition hover:-translate-y-1 hover:border-primary/60"><div className="aspect-[4/3] overflow-hidden bg-muted"><img src={product.image} alt={product.name} className="size-full object-cover transition duration-700 group-hover:scale-105" /></div><div className="space-y-3 p-5"><p className="text-xs uppercase tracking-wider text-primary">{product.category || 'Fazco collection'}</p><h2 className="text-xl font-medium">{product.name}</h2><p className="line-clamp-2 text-sm leading-6 text-muted-foreground">{product.description}</p><div className="flex items-center justify-between pt-2"><span className="font-medium">{formatPrice(product.priceInCents)}</span><span className="text-xs text-muted-foreground">View details</span></div></div></Link>)}</div>}</div>
}
